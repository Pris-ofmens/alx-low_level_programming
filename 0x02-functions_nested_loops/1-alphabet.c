#include "main.h"
#include <unistd.h>

/**
 * print_alphabet - start
 * Description: function that prints alphabet in lowercase
 * Return: 0 
 */

void print_alphabet(void)
{
	char g = 'a';
		while
			(g <= 'z');
		{
			_putchar(g);
			g++;
		}
		_putchar('\n');
}
