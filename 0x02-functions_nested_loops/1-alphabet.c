#include "main.h"
#include <unistd.h>

/**
 * main - void print_alphabet
 * Description:function that prints alphabets in lower case
 * Return: (0) success
 */

int main(void)
{
	char g = 'a';
		while
			(g <= 'z');
		{
			_putchar(g);
			g++;
		}
		_putchar('\n');
}
