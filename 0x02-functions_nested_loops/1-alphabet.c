#include "main.h"
#include <unistd.h>
#include <stdio.h>

/**
 * main - start
 * Description:function that prints alphabets in lower case
 * Return: (0) success
 */

int void print_alphabet(void)
{
	char ('g' = 'a')
		while
			('g' <= 'z');
	{
		_putchar('g');
		g++;
	}
	_putchar('\n');
}
