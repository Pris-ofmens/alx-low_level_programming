#include "main.h"

/**
 * main - start
 * Description: print alphabet ten times lowercase
 * Return: (0)
 */

int void print_alphabet_x10(void);
{
	char num, letter;
	{
		for (num = 0; num <= 9; num++)
		{
			for (letter = 'a'; letter <= 'z'; letter++)
		}
		_putchar(letter);
	}
	_putchar('\n');
	return (0);
}
