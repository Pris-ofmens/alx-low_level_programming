#include <stdio.h>
/**
 * main - A C program that prints with the function printf
 * Return: (0) if the return is 0 then it means successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
